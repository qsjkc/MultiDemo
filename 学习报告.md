# Coze 流式语音对话 Demo 学习报告

## 📋 项目概述

本项目基于 Coze 流式语音对话 WebSocket OpenAPI，实现了一个支持三种交互模式的实时语音对话和识图 Demo：

| 模式            | 功能描述             |
| ------------- | ---------------- |
| 🎤 按键长按对话     | 用户长按按钮录音，松开自动发送  |
| 🔊 VAD 判断自由对话 | 服务端自动检测说话起止，支持打断 |
| 🖼️ 上传图片模式    | 支持图片识别与图文混合对话    |
| ✏️ 文字输入       | 底部输入框打字发送        |

***

## 📅 学习过程记录

### 阶段一：需求分析与技术调研

**任务理解**：

- 基于 WebSocket 流式 API 实现实时语音对话
- 不使用 SDK，直接调用 OpenAPI
- 支持三种交互模式：按键对话、VAD 对话、图片上传

**技术调研**：

1. **Coze API 文档分析**：
   - WebSocket 端点：`wss://ws.coze.cn/v1/chat?bot_id={BOT_ID}`
   - 认证方式：Bearer Token
   - 事件类型：上行（`chat.update`、`input_audio_buffer.append`）、下行（`conversation.audio.delta`、`conversation.message.delta`）
2. **技术栈选型**：
   - 语言：Python 3.9+（生态成熟、音视频处理库丰富）
   - WebSocket：`websocket-client`
   - 音频处理：`pyaudio`（录音/播放）、`numpy`（数据处理）
   - GUI：`tkinter`（内置库，快速开发）
   - 图片处理：`Pillow`（图片编码）

### 阶段二：架构设计与代码实现

**架构设计**：

```
┌─────────────────────────────────────────────────┐
│                    GUI Layer (tkinter)          │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐ │
│  │ 按键模式  │  │ VAD模式  │  │ 图片上传模式   │ │
│  └──────────┘  └──────────┘  └───────────────┘ │
├─────────────────────────────────────────────────┤
│               Audio I/O Thread                  │
│  ┌─────────────┐  ┌──────────────┐            │
│  │ 麦克风采集   │  │ 扬声器播放    │            │
│  └─────────────┘  └──────────────┘            │
├─────────────────────────────────────────────────┤
│           WebSocket Client Thread               │
│  ┌──────────────────────────────────────────┐   │
│  │ 收发事件: append/complete/message.create  │   │
│  │ 接收事件: delta/audio/completed          │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

**核心模块**：

| 模块            | 文件                  | 职责             |
| ------------- | ------------------- | -------------- |
| WebSocket 客户端 | `ws_client.py`      | 连接管理、事件收发、心跳保持 |
| 音频 I/O        | `audio_io.py`       | 麦克风录音、扬声器播放    |
| 事件处理          | `event_handlers.py` | 下行事件解析与分发      |
| 媒体工具          | `media_utils.py`    | 图片编码、音频转换      |
| GUI 界面        | `gui.py`            | 用户交互界面         |
| 配置            | `config.py`         | 参数配置           |

### 阶段三：功能实现与调试

#### 3.1 WebSocket 连接与认证

```python
# ws_client.py - 核心连接逻辑
headers = {"Authorization": f"Bearer {ACCESS_TOKEN}"}
self.ws = WebSocketApp(
    WS_URL,
    header=headers,
    on_open=self._on_open,
    on_message=self._on_message,
    on_error=self._on_error,
    on_close=self._on_close,
)
```

**问题 1**：Token 权限不足（错误码 4101）

- **原因**：创建个人访问令牌时未勾选 `chat` 权限
- **解决**：重新创建令牌，确保勾选 `chat` 权限

#### 3.2 音频采集与播放

```python
# audio_io.py - 录音配置
self.stream = self.pa.open(
    format=pyaudio.paInt16,
    channels=1,
    rate=24000,
    input=True,
    frames_per_buffer=2400,
    stream_callback=self._audio_callback,
)
```

**问题 2**：`NameError: name 'pyaudio' is not defined`

- **原因**：pyaudio 仅在局部导入，回调函数中无法访问
- **解决**：将 `import pyaudio` 移到文件顶部全局导入

#### 3.3 三种交互模式实现

**模式一：按键长按对话**

```python
# gui.py - 按键事件处理
def _on_push_talk_press(self, event):
    self.push_to_talk_pressed = True
    self.recorder.resume()
    # 启动音频发送线程
    self.push_to_talk_thread = threading.Thread(target=self._ptt_audio_loop, daemon=True)
    self.push_to_talk_thread.start()
```

**模式二：VAD 自由对话**

```python
# ws_client.py - 配置 VAD
def send_chat_update(self, turn_detection_type="client_interrupt"):
    event = {
        "event_type": "chat.update",
        "data": {
            "turn_detection": {
                "type": turn_detection_type,
                "prefix_padding_ms": 600,
                "silence_duration_ms": 500,
            },
            ...
        }
    }
```

**模式三：图片上传**

```python
# media_utils.py - 图片编码
def build_image_message(image_path, prompt_text=""):
    b64_data = image_to_base64(image_path, max_size=(1280, 1280), quality=95)
    objects = [
        {"type": "image", "file_url": f"data:image/jpeg;base64,{b64_data}"},
        {"type": "text", "text": prompt_text or "请详细描述这张图片中的内容。"}
    ]
    return objects
```

**问题 3**：图片识别不响应

- **原因**：智能体未配置图片理解插件或视觉模型
- **解决**：在 Coze 平台添加「图片理解」插件，使用豆包视觉模型

**问题 4**：AI 回复只有文字，没有语音（核心问题）

- **现象**：WebSocket 连接正常，文本回复正常显示，但听不到任何 AI 语音
- **排查过程**：
  1. 首先确认音频播放设备正常（通过独立测试脚本验证 24000Hz PCM 播放）
  2. 提升日志级别为 DEBUG，发现 `conversation.audio.delta` 事件**确实在频繁下发**
  3. 在 `on_audio_delta` 中打印 delta 数据长度，发现每次读取的 Base64 字符串**长度为 0**
  4. 输出原始 JSON 事件，发现音频数据的**实际字段名是 `content`，而非预期的 `delta`**

- **根因分析**：Coze 流式 API 的下行事件中，`conversation.audio.delta` 事件的消息结构与 `conversation.message.delta` 一致，音频二进制数据存放在 `data.content` 字段（Base64 编码），同时带有 `content_type: "audio"` 标识，而不是独立的 `data.delta` 字段。代码中错误地从 `data.delta` 读取，导致始终读到空字符串。

- **解决**：修改 `event_handlers.py` 中 `on_audio_delta` 方法，从 `data.content` 读取音频数据；同时在 `on_message_delta` 中增加 `content_type` 过滤，避免将音频 Base64 字符串误当作文本拼接显示。

#### 3.4 语音播放技术细节

通过实际运行采集的数据，语音下发具有以下特征：

**音频参数**（由服务端 `chat.updated` 事件协商确定）：

| 参数 | 值 |
|------|-----|
| 编码格式 | **PCM** (无压缩) |
| 采样率 | **24000 Hz** |
| 位深 | **16 bit** (signed int16) |
| 声道数 | **1** (单声道) |
| 传输编码 | **Base64** (WebSocket JSON 传输) |
| 码率 | 24000 × 2 × 1 = **48,000 bytes/s** (≈384 kbps) |

**下发分包特征**（从实际运行日志提取）：

| 指标 | 数值 |
|------|------|
| 大包字节数 | **≈12,000 bytes** (PCM) = 16,000 chars (Base64) |
| 大包时长 | **250 ms** |
| 小包字节数 | **≈180~260 bytes** (PCM) |
| 小包时长 | **≈4~5 ms** |
| 分布规律 | **每隔一个大包穿插一个小包**，小包为句子/韵律边界标记 |
| 下发速率 | 大包约 **4 个/秒**，持续不间断推送 |

**数据流路径**：

```
Coze服务端 → WebSocket JSON → Base64解码 → PCM字节流 → pyaudio输出
   12KB/chunk      16KB Base64      12KB raw      48KB/s连续写入
   (250ms)                                         (24000×16bit×1ch)
```

**播放端参数**（与下发参数一致，无需转码）：

```python
# audio_io.py - 播放器配置
self.stream = self.pa.open(
    format=pyaudio.paInt16,      # 16位有符号整数
    channels=1,                  # 单声道
    rate=24000,                  # 24kHz采样率
    output=True,                 # 输出模式
    frames_per_buffer=2400,      # 缓冲区100ms
)
```

> **关键发现**：服务端以固定 250ms 间隔下发 12KB 音频块，12KB / 48KB/s = 0.25s，恰好覆盖播放时长，实现无缝流式播放。小包（4ms）用于标记音素/句子边界，帮助前端做口型对齐或字幕同步。

### 阶段四：功能完善与优化

1. **添加文字输入功能**：在底部添加输入框，支持打字发送
2. **完善事件处理**：添加语音识别状态、转文字完成等事件处理
3. **优化日志输出**：添加详细的调试日志，便于排查问题
4. **修复线程安全问题**：确保录音线程和播放线程正确管理

***

## 🔧 技术实现细节

### 4.1 音频流式传输

```python
# 流式发送音频块
def _ptt_audio_loop(self):
    while self.push_to_talk_pressed and self.client.connected:
        chunk = self.recorder.read_chunk(timeout=0.1)
        if chunk:
            self.client.send_audio_chunk(chunk)
```

### 4.2 语音识别事件处理

```python
# event_handlers.py
def on_speech_started(self, data):
    self.gui_callback("status", "正在识别语音...")

def on_transcript_completed(self, data):
    transcript = data.get("data", {}).get("transcript", "")
    self.gui_callback("user_text", transcript)
```

### 4.3 多模态消息格式

```python
# object_string 格式（图片+文字）
[
    {"type": "image", "file_url": "data:image/jpeg;base64,xxx..."},
    {"type": "text", "text": "描述这张图片"}
]
```

***

## 📁 项目文件结构

```
MUltimodalDemo/
├── main.py                 # 程序入口
├── config.py               # 配置文件（Bot ID、Token、音频参数）
├── ws_client.py            # WebSocket 客户端核心
├── audio_io.py             # 音频输入输出模块
├── event_handlers.py       # 事件处理器
├── media_utils.py          # 媒体处理工具
├── gui.py                  # Tkinter GUI 界面
├── requirements.txt        # 依赖清单
└── 学习报告.md             # 本报告
```

***

## 🚀 使用说明

### 前置条件

1. **注册 Coze 账号**：访问 <https://www.coze.cn>
2. **创建智能体**：配置人设、添加图片理解插件、发布到 API 渠道
3. **获取凭证**：
   - Bot ID：智能体页面地址栏中 `bot/` 后的数字
   - Access Token：在 [code.coze.cn](https://code.coze.cn) 创建个人访问令牌（勾选 `chat` 权限）

### 配置与运行

```powershell
# 设置环境变量
$env:COZE_BOT_ID="你的BotID"
$env:COZE_ACCESS_TOKEN="你的Token"

# 安装依赖
pip install -r requirements.txt

# 运行 Demo
python main.py
```

### 功能测试

| 模式     | 操作              |
| ------ | --------------- |
| 按键对话   | 长按绿色按钮说话，松开发送   |
| VAD 对话 | 点击「开始对话」后自由说话   |
| 图片上传   | 选择图片 + 输入提示词后发送 |
| 文字输入   | 在底部输入框打字按回车发送   |

***

## 🎯 学习成果与收获

### 技术能力提升

1. **WebSocket 流式通信**：掌握实时双向数据流处理
2. **音频处理**：理解 PCM 编码、采样率、缓冲区管理
3. **多模态交互**：实现语音、文字、图片的综合交互
4. **线程管理**：处理多线程并发问题

### 问题解决能力

1. **API 认证问题**：学会排查 Token 权限问题（错误码 4101）
2. **音频设备问题**：理解 pyaudio 配置与回调机制（局部导入导致 NameError）
3. **智能体配置问题**：掌握 Coze 平台插件配置与发布流程
4. **音频播放调试能力**：通过逐级日志排查（DEBUG → 数据长度 → 原始JSON），发现 API 字段名与预期不符，准确定位 `data.content` vs `data.delta` 的差异。并进一步实测分析了下行音频的分包规律（250ms/12KB大包 + 4ms边界小包）和码率特征（48KB/s PCM）

### 项目管理能力

1. **需求分析**：将复杂需求拆解为可实现的模块
2. **代码组织**：采用模块化设计，提高可维护性
3. **调试技巧**：通过日志定位问题，逐步排查

***

## 📝 待优化事项

| 优化项    | 优先级 | 说明              |
| ------ | --- | --------------- |
| 音频格式支持 | 中   | 添加 Opus 编解码支持   |
| 断线重连   | 高   | 实现指数退避重连策略      |
| 错误恢复   | 中   | 完善异常处理与状态恢复     |
| UI 美化  | 低   | 使用更好的 GUI 框架或样式 |

***

## 🔗 参考资料

1. **Coze 开发者文档**：<https://www.coze.cn/open/docs/developer_guides/streaming_chat_api>
2. **WebSocket 协议**：[RFC 6455](https://datatracker.ietf.org/doc/html/rfc6455)
3. **PyAudio 文档**：<https://people.csail.mit.edu/hubert/pyaudio/docs/>

***

**完成时间**：2026年5月
